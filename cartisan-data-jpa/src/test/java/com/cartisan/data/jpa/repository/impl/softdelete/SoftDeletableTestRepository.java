package com.cartisan.data.jpa.repository.impl.softdelete;

import com.cartisan.data.jpa.repository.BaseRepository;

/**
 * 软删除实体 Repository。
 */
public interface SoftDeletableTestRepository extends BaseRepository<SoftDeletableTestEntity, Long> {
}
