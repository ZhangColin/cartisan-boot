package com.cartisan.data.jpa.config;

import com.cartisan.data.jpa.repository.BaseRepository;

/**
 * Repository 工厂自动配置测试用 Repository。
 */
interface RepositoryFactoryCustomizerTestRepository extends BaseRepository<RepositoryFactoryCustomizerTestEntity, Long> {
}
