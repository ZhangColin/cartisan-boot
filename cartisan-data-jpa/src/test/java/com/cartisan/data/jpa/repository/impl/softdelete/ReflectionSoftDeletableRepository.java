package com.cartisan.data.jpa.repository.impl.softdelete;

import com.cartisan.data.jpa.repository.BaseRepository;

/**
 * 反射软删除实体 Repository。
 */
public interface ReflectionSoftDeletableRepository extends BaseRepository<ReflectionSoftDeletableEntity, Long> {
}
