package com.cartisan.data.jpa.domain;

import com.cartisan.data.jpa.repository.BaseRepository;

/**
 * {@link TestDirectSoftDeletable} 的 Repository。
 */
interface TestDirectSoftDeletableRepository extends BaseRepository<TestDirectSoftDeletable, Long> {
}
